# Mosco

한 줄 자연어 입력으로 할 일을 적고, 온디바이스 임베딩으로 카테고리를 자동 분류하는
iOS 캘린더/할 일 앱. 1인 개발. App Store 출시됨 (현재 1.1.0, 빌드 4).

## 무엇이 어디에 있나

```
Mosco/Mosco.xcodeproj        프로젝트 (스킴: App, MoscoWidget)
Mosco/App/Features/          Calendar / TodayTodo / Settings
Mosco/App/Common/
  DesignSystem/              토큰·컴포넌트 + README.md ← 디자인·문구 규범
  Tutorial/ ML/ Weather/ Sync/ Notifications/ Analytics/ Review/ Clipboard/
Mosco/MoscoWidget/           위젯 + 라이브 액티비티
Mosco/Shared/                앱·위젯 공용 (Analytics, CategoryColorPalette, LiveActivity)
Mosco/MoscoTests/            유닛 테스트 (Swift Testing). 폴더째 동기화된다

CONTRIBUTING.md              커밋·브랜치·문서 갱신 규칙
RELEASING.md                 버전·태그·릴리스 노트
docs/TRAPS.md                플랫폼 함정. 해당 영역 건드리기 전에 읽는다
docs/CATEGORIZATION.md       카테고리 자동 분류가 어떻게 돌고 왜 Core ML을 버렸나
docs/BACKLOG.md              밀린 일. 목록을 던지면 여기로 들어간다
docs/harness/                이 규칙들이 효과 있었는지 채점하는 체계
tools/harness_report.py      버전별 지표 추출
tools/deadcode_audit.py      안 쓰이는 코드 훑기. periphery는 이 프로젝트에서 못 쓴다
```

배포 타깃 iOS 17.0. 번들 ID `com.Mosco.App`. Swift 88파일 ≈ 11,900줄.
**테스트 타깃 `MoscoTests`가 있다** (Swift Testing, 108건). **CI도 있다** —
PR마다 App·MoscoWidget 빌드와 테스트가 자동으로 돈다
(`.github/workflows/ci.yml`). 그래도 먼저 돌리는 것은 AI다 — CI는 마지막 그물이지
편집 후 확인을 대신하지 않는다 (R1).

> **시뮬레이터는 AI가 직접 쓰지 않는다.** 열지도, 탭하지도, 캡처하지도 않는다.
> 예외는 PR에 넣을 화면을 찍을 때 하나뿐이다. 화면 확인이 필요하면 사용자에게
> 카드로 넘긴다. 자세한 것은 R2. — 이 문장을 지우기 전에 R2를 먼저 읽는다.

## 빌드

```bash
xcodebuild -project Mosco/Mosco.xcodeproj -scheme App -sdk iphonesimulator \
  -destination 'platform=iOS Simulator,name=iPhone 17 Pro' build
```

## 테스트

```bash
xcodebuild -project Mosco/Mosco.xcodeproj -scheme App -sdk iphonesimulator \
  -destination 'platform=iOS Simulator,name=iPhone 17 Pro' test
```

`MoscoTests`는 App 스킴에 물려 있어서 스킴 하나로 빌드와 테스트가 같이 돈다.
파일을 추가할 때 Xcode 프로젝트에 등록하지 않아도 된다 — `MoscoTests/` 폴더가
통째로 동기화된다.

**호스트 앱 없이 돈다.** 테스트 타깃은 `Shared/`를 직접 컴파일하고 App.app을 띄우지
않는다. 앱을 띄우면 CI에서 서명이 없어 저장소를 못 만들고 죽었고, 로컬에서도 매번
CloudKit 오류 로그를 뱉었다. 그래서 **테스트가 닿아야 하는 순수 로직은 `Shared/`에
둔다** — 화면 구조체 안에 두면 테스트를 쓸 수 없다.

UDID를 박아 쓰지 않는다 — 시뮬레이터는 지워지고 다시 생긴다. 이름으로 지정한다.

## 규칙

열 개다. **각 규칙에는 담당 지표가 있고, 버전마다 채점받는다** —
`docs/harness/rules.md`에 도입 시점과 채점 이력이 있다. 두 버전 연속 효과가 없으면
그 규칙은 폐기한다. 지켜지지 않는 문장을 문서에 남겨두지 않는 것이 이 체계의 요점이다.

### 이 규칙들이 무엇을 향하는가 — M12 프롬프트당 토큰

**최종 목표는 같은 결론에 토큰을 덜 쓰고 도달하는 것이다.** 규칙 열 개가 전부
여기로 모이고, 다른 지표가 다 좋아져도 M12가 안 내려가면 그 버전의 하네스는
실패로 적는다.

| | v1.0.0 | v1.1.0 |
|---|---:|---:|
| 프롬프트당 토큰 | 17.1M | **31.1M** |

v1.1.0이 1.8배가 된 이유가 이 체계의 핵심 사례다. 스크린샷을 찍은 그 턴만 비싼 게
아니라 **그 뒤 모든 턴이 부푼 컨텍스트를 다시 읽는다.** 그래서 비용은 행동 하나가
아니라 그 행동이 남긴 컨텍스트에 붙는다 — 무엇을 컨텍스트에 들이는지가 규칙의 대상이다.

### R1 · 빌드와 테스트가 통과하기 전에는 보고하지 않는다
담당 지표 M3 빌드 실패율

편집 후 빌드는 가장 싼 검증이다. 컴파일 에러를 사용자가 발견하는 일이 없어야 한다.

**테스트 타깃이 생겼으므로 이 규칙은 테스트까지 포함한다** (2026-08-18, R7의 예고대로).
로직을 만졌으면 `test`까지 돌리고 그 결과를 적는다. 빌드만 통과한 상태로 "됐습니다"를
쓰지 않는다. 테스트가 깨졌으면 **깨진 채로 보고하지 말고 고치거나, 못 고치면 무엇이
왜 깨졌는지 적는다.**

### R2 · 시뮬레이터를 직접 쓰지 않는다. 검증은 테스트 코드로 남긴다
담당 지표 M11 시뮬레이터 호출 수, M10 회귀 테스트 수

**AI는 시뮬레이터를 열지 않는다. 화면을 탭하지 않는다. 스크린샷을 찍지 않는다.**
`mcp__Claude_Code_iOS_Simulator__*`는 아래 한 가지 예외 말고는 호출 금지다.

> **유일한 예외 — PR 작성.** PR 본문에 UI 변화를 보여줄 화면이 필요하면 그때는
> 직접 찍어 넣는다. 필요한지 아닌지는 AI가 판단한다. UI를 만진 PR이면 대체로 필요하다.

시뮬레이터를 조작해야 확인되는 것은 **사용자에게 요청한다.** 무엇을 어디서 어떤 순서로
하고 무엇이 보이면 통과인지 적은 검증 카드로 넘긴다 (R3, `/handoff`).

**왜 금지인가.** 스크린샷 한 장이 컨텍스트를 크게 먹고, 화면을 찾아가느라 탭·스와이프가
수십 번 쌓인다. v1.0.0에서 스크린샷 391장·시뮬레이터 조작 1,015회, v1.1.0에서
229장·716회였다. 그렇게 써서 얻은 것은 "그려졌다"는 약한 판정 하나뿐이고, 실제 버그는
대부분 사람이 만져볼 때 나왔다. **비싼 자리에서 약한 판정을 사는 거래였다.**

그리고 이 요청은 처음이 아니다 — 2026-07-31, 2026-08-05, 2026-08-18 세 번 나왔다.
앞의 두 번은 문서로 안 남겨서 다음 세션에 잊혔다. 세 번째에 규칙이 됐다.

**대신 무엇을 하나.** 확인하고 싶은 것이 로직이면 테스트를 쓴다 (R7). 화면이면
사용자에게 넘긴다. **"확인했다"고 말할 수 있는 근거는 빌드 결과와 테스트 결과뿐이다.**
그 둘로 덮이지 않는 것은 확인 안 한 것이고, 그렇게 적는다.

### R3 · 다 됐다고 판정하는 것은 사람이다
담당 지표 M1 재지시율, 검증 통과율

빌드와 테스트가 통과해도 **완료라고 쓰지 않는다.** 대신 검증 카드를 넘긴다 — 무엇을,
어디서, 어떤 순서로, 무엇이 보이면 통과이고 무엇이 보이면 실패인지. 절차는 `/handoff`.

"확인해줘"만 보내지 않는다. 그건 무엇을 확인할지 설계하는 일을 사람에게 떠넘기는 것이다.

화면 동작은 전부 카드로 넘어간다 (R2). 특히 이것들은 시뮬레이터가 있어도 판정이
안 되므로 처음부터 카드행이다 — 스크롤 체감·프레임, 푸시 알림 실도착, WeatherKit,
위젯, 라이브 액티비티, 다이나믹 아일랜드, iCloud 동기화, 애널리틱스 DebugView.

> 이 분담은 v1.1.0의 "기계적 확인은 AI, 합격 판정은 사람"에서 바뀐 것이다. 그때는
> 누가 합격을 판정하느냐만 문제로 봤는데, 화면을 탭하는 비용 자체가 컸다. 지금은
> **로직은 테스트, 화면은 사람**이고 AI는 그 사이를 잇는 카드를 쓴다.

### R4 · 성능은 숫자 없이 말하지 않는다
담당 지표 M4 감각 피드백률

"최적화했습니다", "부드러워졌습니다", "개선했습니다"는 측정값이 없으면 쓰지 않는다.
계측이 없으면 **먼저 계측을 넣는 것**이 첫 작업이다 (`OSSignposter`).
측정할 수 없는 상황이면 "측정하지 않았고 이 변경의 근거는 ○○"라고 적는다.

같은 증상을 세 번 고쳤는데도 남아 있으면 네 번째를 시도하지 말고 **설계를 의심한다고
말한다.** 캘린더에서 다섯 번째에 그 말이 나왔고, 그게 정답이었다 (`docs/TRAPS.md`).

### R5 · 목록은 쪼개고, 갈림길은 되묻고, 항목별로 돌려준다
담당 지표 M2 배치 유발 배수

프롬프트의 1/4이 4~7개짜리 목록이다. 한 덩어리로 받아 한 덩어리로 답하면 조용히 일부만
처리되고, 다음 프롬프트가 "아직 안 됐는데"가 된다.

**쪼갠다.** 받은 즉시 항목별로 나눠 먼저 보여준다. 한 줄에 두세 개가 붙어 있는 경우가
많다 — "~수정해주고 그리고 ~도 추가해줘". 그것도 쪼갠다. 이 단계에서 "그건 그런 뜻이
아니었는데"가 나오면 잘못 만든 코드를 되돌리는 것보다 훨씬 싸다.

**되묻는다.** 두 가지로 읽히는 항목은 작업 전에 묻는다. **추측해서 만들지 않는다** —
잘못 읽고 만든 것이 안 만든 것보다 비싸다. 물을 게 둘 이상이면 한 번에 묶어서 묻고,
선택지와 추천을 같이 준다. 관례가 있는 것은 정하고 가되 무엇으로 정했는지 적는다.

**다음 프롬프트를 돕는다.** 되물어야 했다면, 답을 받은 뒤 **어떤 정보가 있었으면
안 물어도 됐는지** 한 줄 알려준다. 잔소리가 아니라 다음 요청을 한 번에 끝내기 위한
것이므로, 한 줄로 짧게 하고 매번 하지 않는다 — 같은 종류의 모호함이 반복될 때만 한다.

> 예: `이 화면` → 캘린더 하루치인지 오늘 할 일 탭인지 갈렸습니다. 다음엔 탭 이름을
> 같이 주시면 바로 갑니다.

**항목별로 돌려준다.** 끝나면 받은 항목 **전부**에 상태를 붙인다:
완료 / 안 함(이유) / 확인 필요. 못 한 것을 빠뜨리는 게 안 한 것보다 나쁘다.

절차는 `/intake`.

### R6 · 규범을 먼저 읽고, 어기게 되면 먼저 말한다
담당 지표 M7 되돌림 사이클, M8 규범 드리프트

UI·문구를 만지기 전에 `Mosco/App/Common/DesignSystem/README.md`를 읽는다.
그 문서는 스타일 가이드가 아니라 **되돌린 시도의 기록**이다 — 한 번 해보고 아니었던
것을 다시 제안하지 않기 위해 있다.

- 새 색·새 코너 반경·새 그림자·새 글라스 표면을 즉석에서 만들지 않는다. 토큰을
  쓰거나, 토큰에 추가하고 **추가했다고 말한다.**
- 글라스는 떠 있는 요소 한 곳에만. 셀·카드·리스트에 쓰지 않는다.
- 색은 한 행에 한 번. "무엇인지"에만 쓰고 "어떤 상태인지"는 진하기로 말한다.
- 문구는 README의 문구 원칙 11개. `~해요`체 하나로 통일.

**코드를 바꿔서 규범 문서가 틀리게 되면 그 자리에서 문서도 고친다.** 틀린 규범은
규칙으로 작동하지 않는다. 이건 M8로 측정된다.

요청이 규범과 부딪히면 그대로 하기 전에 부딪힌다고 한 줄 말한다. 그래도 하라고 하면 한다.

### R7 · 작업물마다 그것을 검증하는 테스트를 남긴다
담당 지표 M10 회귀 테스트 수

R2로 시뮬레이터를 닫았으므로 **테스트가 AI가 쓸 수 있는 유일한 검증**이다. 빌드는
컴파일만 본다. 사람은 정확하지만 비싸고 왕복이 걸린다. 테스트는 그 사이에서 AI가
자기 결과를 객관적으로 채점할 수 있는 하나뿐인 칸이다.

**작업물마다 그것을 검증하는 테스트를 같이 낸다.** 로직을 만졌으면 유닛 테스트,
화면 흐름을 만졌으면 UI 테스트다. "테스트로 못 덮는다"고 판단했으면 그 이유를 적고
검증 카드로 넘긴다 — 조용히 넘어가지 않는다.

**로직 버그를 고쳤으면 그 버그를 재현하는 테스트를 같이 남긴다.** 테스트 이름에
증상을 적는다(`같은_날짜가_월말월초에_두_번_나오지_않는다`). 그래야 다음에 깨졌을 때
무엇이 깨졌는지 이름만 보고 안다.

무엇을 먼저 덮나 — **순수 로직.** 지금까지 반복해서 깨진 곳이 정확히 여기다:

| 영역 | 반복된 증상 |
|---|---|
| 날짜 경계 | 월말/월초에 같은 날짜가 두 번 나옴 |
| 반복 일정 전개 | 3일짜리가 생성일에만 보임 · 하루 완료가 전체 완료로 |
| 블록 배치 | 날짜 아래부터 안 쌓임 · 빈 줄 · 옆으로 밀림 |
| 자연어 시각 파싱 | `4시~7시`의 종료 시각 · 오전/오후 해석 |
| 카테고리 분류 | 임계값 `0.35`에 근거 데이터가 없다 |

**지금까지 덮은 것** (2026-08-19, 108건):

| 스위트 | 무엇을 잡나 |
|---|---|
| `MonthLayoutTests` | 월말/월초 날짜 중복, 격자 연속성, 일요일 시작, 윤년 |
| `RecurrenceTests` | 걸친 날 전개, 하루 완료가 번지지 않음, 며칠마다 간격, id 충돌 |
| `EventRowAssignerTests` | 0행부터 쌓임, 빈 줄 없음, 입력 순서 무관 |
| `TimeExpressionParserTests` | `4시~7시` 종료 시각, 오전/오후 해석, 12시 경계, 중복 매칭 |
| `PermissionGateTests` | 권한 없으면 스위치가 꺼져 보임, 거부 시 설정으로 보냄 |
| `AnalyticsIdentityTests` | 재설치·기기 교체를 건너와도 같은 사람, 값을 안 덮어씀 |
| `TutorialFunnelTests` | 이탈이 다음 실행에 잡힘, 정상 종료는 안 잡힘, 중복 보고 없음 |
| `TimelineLayoutTests` | 겹치는 일정이 나란히 서고 폭을 나눔, 앞 열 재사용, 축 범위 |
| `ThemeColorTests` | 흰색을 골라도 읽히게 눌러 씀, 어두운 색은 그대로, 깨진 값은 기본색 |

아직 안 덮인 것 — **카테고리 분류** 하나다. 임계값 `0.35`에 근거 데이터가 없어서
입력·기대 쌍 30개를 먼저 만들어야 한다 (`docs/TRAPS.md`).

**테스트가 못 닿으면 코드를 옮긴다.** 시각 파싱은 `QuickAddView` 안의
`private static`이라 한 줄도 못 덮었는데, `TimeExpressionParser`로 꺼내니 21건이
붙었다. 화면 구조체 안에 순수 로직을 두면 그건 테스트가 없는 게 아니라 **테스트를
쓸 수 없는 것**이다.

**UI 테스트도 만든다.** v1.1.0에서는 "잘 깨지니 나중에"로 미뤘는데, 그 미룬 자리를
AI가 시뮬레이터를 직접 조작해서 메우고 있었다. 그쪽이 더 비쌌다. 순수 로직을 먼저
덮되, 화면 흐름(첫 실행·튜토리얼·할 일 만들기·삭제 확인)은 UI 테스트로 남긴다.
UI 테스트가 불안정하면 **불안정하다고 적고 고친다** — 그게 사람이 매번 손으로 밟는
것보다 싸다.

테스트 타깃이 생겼으므로 **R1은 이미 "빌드와 테스트가 통과하기 전에는 보고하지
않는다"로 확장돼 있다.**

### R8 · 작업 크기를 먼저 말하고, 위임할 때는 모델을 고른다
담당 지표 M13 상위 모델 비중

AI는 자기 모델을 못 바꾼다. 바꾸는 것은 사용자다. AI가 할 수 있는 것은 둘이다.

- **작업을 시작할 때 크기를 한 줄로 말한다** — 파일 몇 개, 되돌리기 쉬운가, 조사가
  필요한가. 사용자가 모델을 내릴지 판단할 재료를 주는 것이다. 매번 하지 않는다.
  한 파일 고치는 일에 이 줄을 붙이면 그 줄이 낭비다.
- **서브에이전트를 띄울 때는 모델을 명시적으로 고른다** (R9). 파일 찾기·목록 만들기
  같은 것에 상위 모델을 쓰지 않는다.

**싼 모델로 내리는 것 자체가 목표가 아니다.** 잘못 내리면 재작업이 늘고 M1이 오르며,
그러면 토큰은 오히려 더 든다. M13은 그래서 방향이 아니라 맥락 지표다 —
**내렸는데 M1이 안 올랐다**가 성공이다.

### R9 · 넓은 조사는 위임하고, 위임 못 하면 어디서 위임했어야 했는지 적는다
담당 지표 M14 위임 비중

넓은 그물 검색은 결과 전체가 본 컨텍스트에 쌓이고, 그 뒤 모든 턴이 그걸 다시 읽는다.
서브에이전트에 맡기면 **요약만 돌아온다.** v1.0.0·v1.1.0의 위임 비중은 0%였다.

위임이 맞는 일 — 파일 위치 찾기, 명명 규칙 훑기, "이 심볼 쓰는 데 다 찾아줘",
읽어야 결론이 나오지만 읽은 내용 자체는 남길 필요 없는 조사.

위임하면 안 되는 일 — 판단이 필요한 것, 이미 열어둔 파일을 고치는 것, 왕복이
한두 번이면 끝나는 것. 서브에이전트는 맥락 없이 시작하므로 **설명하는 비용이
직접 하는 비용보다 크면 지는 거래**다.

**서브에이전트는 사용자가 요청할 때만 띄운다.** 그래서 이 규칙의 지금 몫은 절반이다 —
띄우지 않은 경우에도 **"여기는 위임했으면 쌌다"를 답 끝에 한 줄 남긴다.** 그 기록이
쌓여야 다음 버전에 상시 위임 구성을 정할 수 있다. 보고서 6절이 그 자리다.

### R10 · 읽는 범위를 좁힌다
담당 지표 M15 전체 읽기 비율

`Read`에 `offset`/`limit`을 안 주면 파일 전체가 컨텍스트로 들어온다. v1.0.0에서
전체 읽기가 67.7%였다.

- **찾을 것을 정한 뒤에 검색한다.** `grep`으로 줄 번호를 먼저 잡고 그 언저리만 읽는다.
- 파일 전체가 필요한 경우가 있다 — 처음 보는 파일의 구조를 알아야 할 때, 200줄 미만일
  때. **그때는 읽는다.** 규칙은 "읽지 마"가 아니라 "왜 전체가 필요한지 알고 읽어라"다.
- 이미 읽은 파일을 다시 읽지 않는다. 편집 도구는 실패하면 에러를 낸다 — 확인하려고
  다시 읽는 것이 가장 흔한 낭비다.

## 일하는 방식

- 사용자는 `auto`/`acceptEdits`로 거의 항상 열어둔다. 편집을 일일이 승인하지 않으므로
  **되돌리기 쉬운 크기**로 나눠 진행한다.
- 프롬프트의 40%가 새벽 0~5시에 나온다. 피곤한 사람이 선택지 다섯 개를 비교하게
  만들지 않는다. **근거 있는 결론 하나**를 준다.
- "너가 10년차 ○○라고 생각하고"가 나오면 원하는 것은 긴 설명이 아니라 **결정**이다.
- 조사를 지시하면 실제로 조사한다. 추측을 조사 결과처럼 쓰지 않는다.
- 커밋은 요청받을 때만. 대신 검증이 끝난 단위마다 **쓸 수 있는 커밋 메시지 한 줄**을
  답 끝에 제안한다. 규칙은 `CONTRIBUTING.md`.
- **토큰을 아끼는 것도 작업의 일부다.** 같은 결론에 이르는 길이 둘이면 싼 쪽으로 간다.
  이건 취향이 아니라 M12로 채점되는 항목이다 (R8·R9·R10).
- **모든 작업은 브랜치와 PR을 거친다.** `main`에 직접 커밋하지 않는다.
  규칙은 `CONTRIBUTING.md`.
