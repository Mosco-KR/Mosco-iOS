# 개발 컨벤션

이 문서는 Mosco-iOS 프로젝트의 커밋 메시지 규칙과 브랜치 전략을 정리합니다.
버전을 어떻게 매기고 App Store에 무엇을 적어 올리는지는
[RELEASING.md](RELEASING.md)에 있습니다.

## 커밋 메시지 컨벤션

[Conventional Commits](https://www.conventionalcommits.org/) 형식을 따릅니다.

```
<type>(<scope>): <subject>

<body>

<footer>
```

- **type** (필수)
  - `feat`: 새로운 기능 추가
  - `fix`: 버그 수정
  - `refactor`: 동작 변화 없는 코드 개선/구조 변경
  - `style`: 코드 포맷팅, 세미콜론 등 스타일 변경 (로직 변경 없음)
  - `docs`: 문서 수정 (README, 주석 등)
  - `test`: 테스트 코드 추가/수정
  - `chore`: 빌드 설정, 패키지 관리, 기타 잡일
  - `perf`: 성능 개선
  - `ci`: CI/CD 설정 변경
- **scope** (선택): 변경 범위. 화면명, 모듈명 등 (예: `login`, `network`)
- **subject** (필수): 무엇을 했는지 명령문으로, 마침표 없이, 50자 이내
  - 예: `feat(login): 소셜 로그인 버튼 추가`
- **body** (선택): 왜 이렇게 변경했는지, 무엇이 달라지는지 설명. 한 줄 요약으로 부족할 때만 작성
- **footer** (선택): 관련 이슈 참조 (`Closes #12`), Breaking Change 명시

### 예시

```
feat(home): 홈 화면 무한 스크롤 적용

페이지네이션 API 연동 완료. 스크롤 하단 도달 시 다음 페이지 로드.

Closes #24
```

```
fix(auth): 토큰 만료 시 재로그인 화면으로 이동하지 않던 버그 수정
```

## 브랜치 전략

**GitHub Flow**를 따릅니다. `main`은 항상 배포 가능한 상태를 유지합니다.

- `main`: 안정 브랜치
- `feature/<설명>`: 새 기능 개발 (예: `feature/login-screen`)
- `fix/<설명>`: 버그 수정 (예: `fix/crash-on-launch`)
- `refactor/<설명>`: 리팩터링
- `chore/<설명>`: 설정, 의존성 등 기타 작업

### 모든 작업은 브랜치와 PR을 거칩니다

`main`에 직접 커밋하지 않습니다. 예외 없습니다 — 문서 한 줄 고치는 것도 브랜치를 팝니다.

1. `main`에서 브랜치 생성
2. 작업 후 커밋 (컨벤션 준수)
3. PR 생성 → 셀프 리뷰 후 `main`에 머지
4. 머지된 브랜치는 삭제

**AI도 예외가 아닙니다.** 작업을 시작할 때 브랜치부터 만들고, 커밋을 요청받으면
PR까지 엽니다. `main`에서 작업 중인 것을 발견하면 그 시점에 브랜치로 옮깁니다.

#### 이 규칙은 한 번 폐기됐다가 되살아난 것입니다

v1.1.0 보고서에서 **"작은 단위로 PR을 나눈다"를 폐기**했습니다. 이유는 "전 기간 한
번도 지켜지지 않았으니 지켜지지 않는 문장을 남겨두면 나머지 규칙까지 신뢰를 잃는다"
였고, 그 자리에 "이럴 때만 브랜치" 표를 넣었습니다.

**2026-08-18에 되살립니다.** 폐기 판단이 틀렸던 게 아니라 **전제가 바뀌었습니다.**

| 그때 | 지금 |
|---|---|
| PR을 열 이유가 셀프 리뷰뿐이었다 | PR이 **AI가 스크린샷을 찍는 유일한 자리**다 (R2) |
| 리뷰어가 없으니 형식 비용만 남았다 | PR 본문이 검증 카드가 도착하는 자리다 (R3) |
| 이력이 커밋에만 남았다 | 무엇을 왜 바꿨는지가 **버전 보고서의 원본**이 된다 |

되살리면서 조건을 하나 바꿨습니다 — **"작은 단위로 나눈다"는 여전히 요구하지
않습니다.** 그게 지켜지지 않았던 부분이기 때문입니다. 요구하는 것은 **브랜치와 PR을
거친다** 하나뿐이고, 그 안에 커밋이 몇 개든 상관하지 않습니다.

이것도 지켜지지 않으면 다시 폐기합니다. 판정은 `docs/harness/rules.md`에서 합니다.

## PR

[.github/pull_request_template.md](.github/pull_request_template.md)를 씁니다.
PR 본문은 **무엇을 왜 바꿨는지**로 시작합니다 — 이 문장이 그대로 버전 보고서의
재료가 되므로, 커밋 메시지를 다시 붙여넣지 말고 이 변경이 무슨 문제를 풀었는지 씁니다.

체크리스트의 "관련 테스트 통과"는 테스트 타깃이 생길 때까지 **"`/verify` 해당 절을
카드로 넘김"**으로 읽습니다. 훑는 것은 AI가 아니라 사람입니다 (`CLAUDE.md` R2).

**PR은 AI가 시뮬레이터 스크린샷을 찍을 수 있는 유일한 자리입니다.** UI를 만진 PR이면
대체로 붙이는 게 맞고, 필요한지는 AI가 판단합니다. 검증 카드나 일반 보고에는 붙이지
않습니다 — 왜 그렇게 정했는지는 [`docs/harness/rules.md`](docs/harness/rules.md)의
폐기 절에 있습니다.

## 커밋

- 한 커밋은 한 가지 일만 합니다. 라이브 액티비티 작업이 `tutorial` 제목의 커밋에 섞여
  들어간 적이 있는데, 릴리스 노트를 git 기록으로 쓰기 때문에 그때 그 작업이 사라졌습니다.
- 제목은 **무엇이 달라지는지**를 씁니다. `fix(calendar): 셀 수정`이 아니라
  `fix(calendar): 날짜 칸의 터치 영역이 숫자 주변에만 잡히던 문제`.
- 커밋 시점은 **검증이 끝난 단위**입니다. 빌드만 통과한 상태로 찍지 않습니다.
- 되돌린 시도는 커밋에 남기지 않습니다. 통한 것 하나만 남깁니다.

## 문서를 언제 고치나

문서가 코드보다 뒤처지면 규범으로 작동하지 않습니다. 아래는 **코드를 바꾼 그 작업
안에서** 같이 고칩니다. 나중에 몰아서 고치지 않습니다.

| 무엇을 바꿨나 | 같이 고칠 것 |
|---|---|
| 디자인 토큰·컴포넌트 추가·삭제·이름 변경 | `DesignSystem/README.md` |
| 화면 문구 규칙에 영향 있는 변경 | `DesignSystem/README.md` 문구 원칙 |
| 애널리틱스 이벤트 추가·삭제 | `PRIVACY.md` 수집 항목 |
| 새로 알아낸 플랫폼 함정 | `docs/TRAPS.md` |
| 기능 추가·삭제 | `README.md` 기능 목록 |
| 버전·빌드 번호 | `RELEASING.md` 절차대로 네 곳 전부 |
| **AI 사용 보고서·하네스 문서** | **GitHub 위키** — 아래 참조 |

### 위키는 저장소의 사본입니다

`docs/harness/reports/`와 하네스 설명은 [GitHub 위키](https://github.com/Mosco-KR/Mosco-iOS/wiki)에도
올라가 있습니다. **저장소가 정본이고 위키는 사본입니다.** 자동 동기화가 없으므로
한쪽만 고치면 갈라집니다 — 실제로 2026-08-18에 저장소만 고치고 위키를 잊은 적이
있습니다.

보고서나 하네스 문서를 고치면 **같은 작업 안에서** 위키도 밀어넣습니다.

```bash
git clone https://github.com/Mosco-KR/Mosco-iOS.wiki.git
```

| 저장소 | 위키 페이지 |
|---|---|
| `docs/harness/reports/v<버전>.md` | `AI-사용-보고서-v<버전>` |
| `docs/harness/README.md` · `rules.md` 요약 | `Home` |

위키로 옮길 때 **상대 링크(`../rules.md`)는 깨집니다.** 절대 URL로 바꾸고, 페이지
맨 위의 "이 페이지는 사본입니다" 안내는 유지합니다.

규범 문서가 실존하지 않는 파일이나 타입을 가리키고 있으면 지표 M8로 잡힙니다
(`docs/harness/`). 0을 유지하는 것이 목표입니다.
