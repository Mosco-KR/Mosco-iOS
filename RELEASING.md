# 릴리스

버전을 어떻게 매기고, 무엇을 준비해 올리는지. 커밋·브랜치 규칙은
[CONTRIBUTING.md](CONTRIBUTING.md)에 있습니다.

## 버전 규칙

`MARKETING_VERSION`은 `MAJOR.MINOR.PATCH`를 씁니다.

| 자리 | 올리는 때 | 올리지 않는 때 |
| --- | --- | --- |
| **MAJOR** | 이 앱이 **무엇인지**가 바뀔 때. 데이터 마이그레이션이 필요할 때. 쓰던 사람이 처음부터 다시 배워야 할 때 | 기능을 여러 개 더한 것만으로는 올리지 않습니다 |
| **MINOR** | 기능이 늘거나 화면 구조가 바뀔 때. 저장 스키마가 그대로라면 대개 여기 | |
| **PATCH** | 버그 수정, 문구 수정, 성능 개선만 있을 때 | 기능이 하나라도 늘면 MINOR |

**판단이 갈리면 낮은 쪽으로 갑니다.** 버전은 광고 문구가 아니라 "무엇이
달라졌는지"에 대한 약속입니다. 2.0을 붙이면 사람들은 다른 앱이 됐다고 읽고,
그만큼 실망할 여지도 커집니다.

### 빌드 번호

`CURRENT_PROJECT_VERSION`은 **심사에 올린 빌드마다 1씩 올리고, 버전이 바뀌어도
되돌리지 않습니다.** 버전 문자열이 새로 시작하면 1부터 다시 매겨도 App Store
Connect가 받아주긴 하지만, 그러면 목록에서 어느 빌드가 먼저인지 사람이 날짜를
보고 맞춰야 합니다. 계속 올라가기만 하면 그 일이 없습니다.

### 앱과 위젯은 같은 버전이어야 합니다

익스텐션 버전이 앱과 다르면 업로드가 거부됩니다. `project.pbxproj`에
`MARKETING_VERSION`이 **네 곳**(App·MoscoWidget × Debug·Release) 있으니 네 곳을
모두 고칩니다. 빌드 번호도 마찬가지입니다.

```bash
grep -n "MARKETING_VERSION\|CURRENT_PROJECT_VERSION" Mosco/Mosco.xcodeproj/project.pbxproj
```

## 태그

- 이름은 `V<버전>` (예: `V1.1.0`)
- **annotated 태그**로 만들고(`git tag -a`), 메시지에 릴리스 노트 요약을 넣습니다.
  가벼운 태그는 누가 언제 왜 찍었는지가 남지 않습니다
- 심사에 제출한 그 커밋에 찍습니다

## 릴리스 노트 쓰는 법

App Store의 "새로운 기능" 칸에 들어가는 글입니다. 규칙은 넷입니다.

1. **첫 줄은 기존 사용자가 당황할 변화 하나.** 새 기능이 아니라. 업데이트하고
   앱을 열었을 때 "어? 그게 어디 갔지"가 생기는 것이 있으면 그게 첫 줄입니다.
2. **세 덩어리로 나눕니다** — 새로 생긴 것 / 더 편해진 것 / 고친 것.
3. **내부 용어를 쓰지 않습니다.** 라이브 액티비티·리팩터링·지표 같은 말 대신
   무엇이 달라지는지를 씁니다("잠금화면에 남은 시간이 떠요").
4. **분석·지표 수정은 적지 않습니다.** 사용자에게 일어나는 일이 없습니다.

## 절차

1. `main`에서 빌드가 통과하는지 확인
2. `project.pbxproj`의 버전 네 곳 + 빌드 번호 네 곳 수정
3. **[PRIVACY.md](PRIVACY.md)의 수집 항목이 실제와 맞는지 확인** —
   `Shared/Analytics.swift`의 이벤트 목록과 대조합니다. 이벤트를 더하거나 뺀
   릴리스라면 이 문서도 같이 나가야 합니다
4. `chore(release): <버전> (빌드 <n>)` 커밋. 본문에 무엇이 달라졌는지와
   **그 버전 자리를 고른 이유**를 남깁니다
5. `git tag -a V<버전>`
6. 아카이브 → App Store Connect 업로드 → 아래 노트를 붙여넣기

---

## 버전별 릴리스 노트

### 1.1.0 (빌드 4) — 2026-08-13

```
목록 탭이 하나로 합쳐졌어요.
'오늘'과 '다가오는'을 오갈 일이 없어졌어요. 지난 할 일과 오늘 할 일이 한 화면에
있고, 앞으로의 계획은 달력에서 봅니다.

새로 생긴 것
• 잠금화면과 다이나믹 아일랜드에 남은 시간이 떠요. 거기서 바로 완료할 수 있어요.
  (목록에서 할 일을 꾹 눌러 켜세요)
• 처음 앱을 열면 1분짜리 사용법을 함께 따라가 볼 수 있어요. 건너뛰어도 설정에서
  다시 볼 수 있어요.
• 할 일을 복사해 다른 날에 붙여넣을 수 있어요.
• 대체공휴일이 달력에 함께 표시돼요.

더 편해진 것
• 할 일 칸을 한 번만 톡 누르면 완료돼요. 다시 누르면 취소되고요.
• 메모를 열어보지 않아도 칸에서 바로 보여요.
• 달력에서 날짜를 누르면 그날 하루가 열려요. 뒤로 밀면 달력으로 돌아옵니다.
• iCloud 백업이 되고 있는지 설정에서 확인할 수 있어요.

고친 것
• 할 일 오른쪽 버튼을 누르면 바로 수정으로 갑니다.
• 일정을 정하고 '완료'를 누르면 그 자리에서 반영돼요.
```

**1.1.0을 고른 이유**: 기능이 셋 늘었지만(잠금화면 표시·사용법 안내·복사
붙여넣기) 저장 스키마가 그대로고 마이그레이션이 없습니다. 쓰던 사람이 다시
배워야 하는 것은 "목록 탭이 하나로 줄었다" 하나뿐이라 MAJOR가 아닙니다.

### 1.0.0 — 2026-08-07

첫 출시.
