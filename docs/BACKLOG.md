# 밀린 일

목록을 던지면 여기로 들어온다. 세션이 끝나면 채팅은 사라지지만 이 파일은 남는다.

**규칙**
- 한 줄에 하나. 하려는 것만 적고, 어떻게 할지는 적지 않는다.
- 상태는 셋뿐이다: `[ ]` 안 함 / `[~]` 하는 중 / `[x]` 확인까지 끝남.
- `[x]`는 **직접 보고 확인했을 때만** 붙인다. 코드를 넣은 것과 되는 것은 다르다.
- 안 하기로 한 것은 지우지 말고 `버림` 절로 옮기고 이유를 한 줄 적는다. 같은 걸
  두 달 뒤에 다시 꺼내는 일을 막는다.

아래는 2026-08-18 기준, 지난 세션들의 목록에서 **커밋으로 확인되지 않은 것**을 모은
것이다. 이미 됐는데 여기 남아 있는 게 있으면 확인 후 `[x]`로 옮긴다.

## 확인이 필요한 것 (했는지 불확실)

- [ ] 할 일 목록에서 입력창이 안 나오는 버그 — 2026-08-02에 "일단 넘어가"로 미뤘고 그 뒤 언급 없음
- [ ] 삭제 후 데이터가 날아가는 현상 — 2026-08-10 목록에 있었으나 대응 커밋이 안 보임
- [ ] 태그가 여러 개라 길어질 때 두 줄로 흐르기 — `WrappingHStack`은 있는데 적용 여부 미확인
- [x] 앱 시작 시 알림 권한 요청 — `StartupPermissionRequest`. 안내가 끝난 뒤
      알림 → 위치 순서로 묻는다 (2026-08-18)
- [ ] 앱스토어 리뷰 유도가 실제로 뜨는지 — 2026-08-05에 넣었고 08-11에 "잘 안 되는 것 같다"

## 안 한 것

- [x] 대체 공휴일 표시 — 시뮬레이터에서 2026-08-17이 빨간 '대체공휴일'로 나오는 것을
      직접 봤다 (2026-08-18)
- [ ] 영어 지원 (현재 기본 언어 한국어 전용으로 좁혀둠)
- [ ] 애플 워치 지원
- [ ] 결제 모듈
- [ ] 가격 A/B 테스트
- [ ] 수출 규정 문서 이슈 — `Info.plist`에 `ITSAppUsesNonExemptEncryption` 넣어 해소
- [ ] 카테고리 분류 기준선 만들기 — 입력·기대 카테고리 쌍 30개. `docs/TRAPS.md` 참고

## 하네스를 만들면서 드러난 것

- [x] `CONTRIBUTING.md`가 feature 브랜치 + PR 절차인데 실제로는 `main` 직행이던 것 —
      실제 기준(언제 브랜치, 언제 main)을 표로 정하고 "작은 단위 PR" 규칙은 폐기했다 (2026-08-18)
- [x] `DesignSystem/README.md`가 없는 파일(`PriorityTag`, `DemoPriority`)과 틀린 hex 값을
      가리키던 것 — 팔레트·컴포넌트 표를 실제 코드에 맞춰 고쳤다. M8로 계속 감시된다 (2026-08-18)
- [x] `README.md`가 11바이트 스텁이던 것 — 기능·구조·문서 지도를 채웠다 (2026-08-18)
- [x] PR 템플릿의 "관련 테스트 통과" — 테스트 타깃이 생길 때까지 `/verify` 훑음으로
      읽는다고 `CONTRIBUTING.md`에 명시했다 (2026-08-18)
- [x] 커밋 하나에 여러 작업이 섞이던 것 — `CONTRIBUTING.md` 커밋 절에 규칙을 넣었다 (2026-08-18)

## 죽은 코드 훑기에서 나온 것 (2026-08-18)

- [x] Xcode 템플릿 보일러플레이트 — `SceneDelegate` 빈 메서드 5개, `AppDelegate`
      `didDiscardSceneSessions`. 본문 없이 영문 주석만 있던 것을 걷어냈다
- [x] 버린 MoSCoW 우선순위 색 `should`/`could`/`wont` — 참조 0곳. `must`만 남겼다
      (공휴일 이름·과부하 배너). `DesignSystem/README.md` 팔레트 표도 같이 고쳤다
- [x] 미참조 토큰·프로퍼티 — `Metrics.chipRadius`, `Metrics.listSectionGap`,
      `MonthPageMetrics.compactRowHeight`, `CategoryColorPalette.colors`,
      `TodayTodoWidgetView.remaining`, `MonthLayout.weekRowIndex(of:)`
- [x] 미사용 `import SwiftData` 3개 — `CategoryEditorSheet`, `TodoDetailSheet`,
      `ToggleTodoCompletionIntent`
- [x] `Mosco/build/` 18MB 작업 폴더 삭제 (BACKLOG 이전 항목 해소)
- [ ] **카테고리 편집에 "알림 권한 거부" 안내가 없다** — `showsDeniedNotice`라는
      `@State`만 있고 읽는 곳이 없었다(그래서 지웠다). 알림을 켜도 시스템 권한이
      거부돼 있으면 사용자는 아무 말도 못 듣는다. 안내를 넣을지 정해야 한다
- [ ] `MoscoPalette.must` 이름이 기능과 안 맞는다 — 우선순위는 없어졌고 지금은
      경고 빨강이다. `alert` 같은 이름이 맞지만 호출부 3곳을 건드려야 해서 미뤘다
- [ ] `DayCell`의 공휴일 라벨이 `MoscoPalette.must`를 쓴다 — `CalendarDayTone`은
      "달력 색과 팔레트 색을 일부러 분리한다"고 적어놨는데 여기서 섞였다.
      값은 같아서(`#EF4444`) 화면은 그대로다. `CalendarDayTone.holidayRed`로 옮길 것
- [x] `MoscoML.mlproj`(1.4MB)·`MLTraining/` 삭제 — 갈아탄 이유를
      `docs/CATEGORIZATION.md`에 적고 파일은 지웠다. 남길 값어치는 파일이 아니라
      이유였다. 되살릴 일이 생기면 git 이력에 있다 (2026-08-19)
- [ ] `StyleGuideView` 진입점이 없다 — `#Preview`로만 열린다. 설정에 디버그 진입점을
      두거나, 규범 확인은 Xcode 프리뷰로만 한다고 정하거나
## v1.2.0 첫 작업 — 테스트 타깃 (R2 · R7)

**우선순위가 올라갔다 (2026-08-18).** R2가 시뮬레이터를 닫으면서 테스트가 AI가 쓸 수
있는 유일한 검증이 됐다. 이게 되기 전까지 AI가 확인할 수 있는 것은 컴파일뿐이다.

순서대로 한다. 위에서부터 반복해서 깨진 순이다.

- [x] **테스트 타깃 생성** — `MoscoTests`, Swift Testing, TEST_HOST = App. App 스킴에
      물려서 스킴 하나로 빌드와 테스트가 같이 돈다 (2026-08-18)
- [x] 날짜 경계 — 월말/월초에 같은 날짜가 두 번 나오지 않는다 · `MonthLayoutTests` 8건
- [x] 반복 일정 전개 — 3일짜리가 걸친 날짜 전부에 나온다 · `RecurrenceTests`
- [x] 반복 일정 완료 — 하루 완료가 다른 날 완료로 번지지 않는다 · `RecurrenceTests`
- [x] 블록 배치 — 날짜 바로 아래부터 쌓이고 빈 줄이 없다 · `EventRowAssignerTests` 9건
- [x] **시각 파싱** — `TimeExpressionParser`로 꺼내고 `TimeExpressionParserTests`
      21건으로 덮었다. `4시~7시` 종료 시각, 오전/오후 해석, 오전/오후 12시 경계,
      `오후 7시`가 `N시` 패턴에 중복으로 안 잡히는 것까지 (2026-08-18)
- [ ] 분류 기준선 — 입력·기대 카테고리 쌍 30개로 임계값 `0.35` 검증 (`docs/TRAPS.md`)
- [x] `CLAUDE.md` R1을 "빌드와 **테스트**가 통과하기 전에는 보고하지 않는다"로
      확장 (2026-08-18)

순수 로직을 덮은 뒤 화면 흐름을 UI 테스트로 남긴다 — 첫 실행·튜토리얼·할 일
만들기·삭제 확인 순. `/verify` 목록에서 테스트로 옮긴 항목은 그 목록에서 지운다.

- [ ] UI 테스트 — 첫 실행과 튜토리얼 건너뛰기
- [ ] UI 테스트 — 한 줄 입력으로 할 일이 만들어진다
- [ ] UI 테스트 — 삭제 확인 뒤 다른 데이터가 남아 있다

## 2026-08-18 목록 (CI/CD · 튜토리얼 데이터 · 뷰 모드 · 권한)

받은 즉시 적는다. 착수 순서는 갈림길 답을 받고 정한다.

### CI/CD
- [ ] PR마다 빌드 + 테스트 자동 실행
- [ ] 배포 자동화 범위 미정 — TestFlight까지인지, 스토어 제출까지인지
- [ ] **막는 것**: `GoogleService-Info.plist`가 저장소에 없다. 서명은
      `DEVELOPMENT_TEAM = W5QTRMS954`로 박혀 있다. 둘 다 Secrets 처리가 필요하다

### 튜토리얼 데이터
- [ ] **이탈이 안 잡힌다** — `tutorialEnded`의 reason은 `skipped`/`finished` 둘뿐이다.
      앱을 죽이거나 백그라운드로 두고 안 돌아오면 아무 기록도 안 남는다
- [ ] 완료·스킵은 이미 기록된다 (`TutorialCoordinator.swift:135,143`)
- [ ] **사용자 식별이 아예 없다** — `setUserID` 호출 0건. 재설치하면 다른 사람이 된다

### 시간표 뷰 모드
- [ ] 캘린더 하루치 화면과 오늘 할 일 탭에 시간표 보기 추가
- [ ] 마지막으로 고른 뷰 모드가 유지된다 (`@AppStorage` 패턴이 이미 있다)
- [ ] `TodoItem.startTime`/`endTime`이 있으므로 시간축 배치는 가능하다.
      **시간 없는 할 일을 어디에 둘지**가 미정

### 권한
- [x] **설정 토글이 권한 상태를 안 따른다** — `PermissionGate`(순수 로직, 테스트 11건)로
      판정을 옮기고 알림·라이브 액티비티·날씨 세 토글을 전부 물렸다. 권한이 없으면
      꺼진 것으로 보이고, 거부 상태에서 켜려 하면 시스템 설정으로 보낸다 (2026-08-18)
- [x] 앱 시작 시 필요한 권한을 모두 요청 — `StartupPermissionRequest`로 되살렸다.
      알림 → 위치 순서로 줄 세워 묻는다 (2026-08-18)
- [x] 튜토리얼과 겹치는 순서 — **안내가 끝난 뒤에 묻는다.** 앱을 한 번 보여준 뒤에
      물어야 무엇에 쓰는 권한인지 알고 고를 수 있고, 한 번 거부되면 다시 못 묻는다

## 불필요 코드 전수조사 (2026-08-19)

`tools/deadcode_audit.py`로 훑었다. **Swift 코드는 깨끗하다** — 미참조 심볼 0건,
통째로 죽은 파일 0건, 미발생 이벤트 0건, 미사용 에셋·문자열 0건.
남은 낭비는 코드가 아니라 타깃 밖 파일이었다.

- [x] `MoscoML.mlproj` + `MLTraining/` 삭제 (1.4MB)
- [ ] `screenshot/` 5장 3.3MB — 코드·문서 어디서도 참조하지 않는다.
      App Store 제출용 원본이면 남기고, 아니면 지운다. **확인 필요**
- [x] periphery(표준 도구)는 이 프로젝트에서 못 쓴다 — Xcode 16 폴더 동기화
      그룹을 이해하지 못해 소스 목록이 빈 채로 돌고 "깨끗하다"고 답한다.
      죽은 코드를 심어 확인했다

## 버림

<!-- 안 하기로 한 것 + 이유. 예: 라이브 액티비티 원격 자동 시작 — 서버가 필요해서 보류 -->

- 라이브 액티비티 원격 자동 시작 — `pushToStartToken`에 서버가 필요하다. 서버는 넣지
  않기로 해서, 꾹 눌러 직접 시작하는 방식으로 대체했다 (2026-08-12)
- Create ML 우선순위 분류기 — 온디바이스 임베딩 + 코사인 유사도로 교체했다.
  학습 과정이 없어 카테고리를 사용자가 추가해도 즉시 대응된다 (2026-07-30)
